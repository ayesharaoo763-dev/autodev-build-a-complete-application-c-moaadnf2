const subjectList = document.getElementById('subject-list'); const topicList = document.getElementById('topic-list'); const scheduleList = document.getElementById('schedule-list'); const addSubjectButton = document.getElementById('add-subject'); const addTopicButton = document.getElementById('add-topic'); let subjects = []; let topics = []; let schedule = []; addSubjectButton.addEventListener('click', () => { const subjectName = prompt('Enter subject name'); if (subjectName) { subjects.push(subjectName); renderSubjectList(); } }); addTopicButton.addEventListener('click', () => { const topicName = prompt('Enter topic name'); if (topicName) { topics.push(topicName); renderTopicList(); } }); function renderSubjectList() { subjectList.innerHTML = ''; subjects.forEach((subject) => { const li = document.createElement('li'); li.textContent = subject; subjectList.appendChild(li); }); } function renderTopicList() { topicList.innerHTML = ''; topics.forEach((topic) => { const li = document.createElement('li'); li.textContent = topic; topicList.appendChild(li); }); } function renderScheduleList() { scheduleList.innerHTML = ''; schedule.forEach((item) => { const li = document.createElement('li'); li.textContent = item; scheduleList.appendChild(li); }); }