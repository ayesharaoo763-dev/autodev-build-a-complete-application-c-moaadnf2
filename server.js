const express = require('express'); const app = express(); const mongoose = require('mongoose'); mongoose.connect('mongodb://localhost/smart-study-planner', { useNewUrlParser: true, useUnifiedTopology: true }); const subjectSchema = new mongoose.Schema({ name: String }); const topicSchema = new mongoose.Schema({ name: String }); const scheduleSchema = new mongoose.Schema({ date: Date, subject: String, topic: String }); const Subject = mongoose.model('Subject', subjectSchema); const Topic = mongoose.model('Topic', topicSchema); const Schedule = mongoose.model('Schedule', scheduleSchema); app.use(express.json()); app.post('/api/subjects', (req, res) => { const subject = new Subject(req.body); subject.save((err) => { if (err) { res.status(500).send(err); } else { res.send({ message: 'Subject added successfully' }); } }); }); app.post('/api/topics', (req, res) => { const topic = new Topic(req.body); topic.save((err) => { if (err) { res.status(500).send(err); } else { res.send({ message: 'Topic added successfully' }); } }); }); app.post('/api/schedule', (req, res) => { const scheduleItem = new Schedule(req.body); scheduleItem.save((err) => { if (err) { res.status(500).send(err); } else { res.send({ message: 'Schedule item added successfully' }); } }); }); app.listen(3000, () => { console.log('Server started on port 3000'); });